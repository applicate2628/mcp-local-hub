# Roadmap

1. Recover and bind the accepted own-server decision. **PASS**
2. Implement the six-tool job/provenance MVP against installed AEDT and CST API surfaces. **PASS**
3. Verify protocol, consent gates, precision, provenance, packaging, and installed API boundaries. **PASS**
4. Publish the server source and add exact-SHA mcphub catalog entries. **PASS**
5. Merge, deploy, and prove the hub and both entries operational. **PASS**
