# RESUME STATE — parked 2026-06-13 ~10:55Z (3h limit-refresh park)

## #288 loop position
- master/PR-head v0.6-core-review = 5f98aa0 (r33 landed: 10 fixes).
- Bot r33 = COMMENTED, 4 new P2 (upgrade-routing cluster), all confirmed vs code.
- r34 IN FLIGHT: 2 opus lanes (X=a9be7eb8 upgrade-routing install.go+upgrade.go r33-1/3/4;
  Y=a17fd47 sibling-aware ownership install_parsed_manifest.go r33-2). Worktrees r34-laneX/Y.
  WHEN THEY FINISH: squash X+Y -> composition-verify (build x2 GOOS, vet, gofmt, narrow api+cli tests,
  live-state SHA must stay 90ee5a757465fb2462f2da454685ccdf4476168d87d237f3347f9cf72e692c7b)
  -> push origin master + master:v0.6-core-review -> gh pr comment 288 "@codex review" -> poll r34.
  NOTE: Lane X FIX 3 (r33-4 no-client-rewrite) may report it needs an api-side InstallOpts knob
  (out of cli scope) -> small follow-up if so.

## Decisions LOCKED (user 2026-06-13)
- SEC-F2 (--strict-mode flag inert: unify intent.strict_mode + env gate, make flag actually enforce)
  + SEC-F3 (Windows kill gates owner-SID check, mirror POSIX UID gate) -> ONE security-PR.
- ROOT orphan-reap (supervisor never reaps vanished descriptor; design panel spec in
  .scratch tasks/wl3oq23de) -> SEPARATE PR.
- Both follow-up PRs land AFTER #288 reaches bot PASS.

## On resume (~3h, ~14:00Z): user said "several more security PRs will arrive -> merge ALL into ONE"
1. Land r34 if not yet (above).
2. Drive #288 bot loop to PASS (fix every finding, opus lanes, NO codex per user).
3. gh pr list / check incoming security PRs (#292+?) -> consolidate ALL into ONE common-flow PR
   (same as #284-#291 merge pattern) + fold SEC-F2+SEC-F3 into that security-PR.
4. On #288 PASS: gh pr close 288 (REVIEW-ONLY, not merge) + cleanup + redeploy (SEC-F1 = deploy-pain cure).

## Constraints (standing)
- NO codex launches until user says; all impl via OPUS subagents.
- Bot PASS required before any merge; never --admin.
- Redeploy always after binary change. Never blanket-kill mcphub.exe/node.exe.
- Live fleet: supervisor PID 34404; back up supervisor-intent.json before any test that could touch state.
- Russian responses, caveman full.

## INCIDENT 2026-06-13 11:05Z — test stopped the live fleet (RESTORED 11:28Z)
- ROOT (audit-log): a `cli.test.exe` ran real `mcphub stop --all` at 11:05:03Z (auditWhoMcphubStopAll,
  install_intent.go:111) → user-stopped all 22 daemons. Triggered by Lane X verify run
  `go test ./internal/cli/ -run 'Upgrade|...|Migration'` hitting a test that reaches real `a.StopAll()`
  unmocked + with NO state-dir isolation. Green on CI (no fleet), DESTRUCTIVE on dev machine.
- RESTORED: `mcphub restart --server <X>` clears user-stop + respawns. Did all 10 stdio daemons
  (memory/time/sequential-thinking/wolfram/paper-search-mcp/fetch/gdb/godbolt/lldb/perftools).
  claude mcp list = all 12 ✔ Connected. serena/LSP were on-demand (router clears their stop on wake).
- NEW baseline: supervisor-intent.json.restored-bak (SHA 2e2686f5...).

## MUST-FIX (fleet hazard, pre-existing, on master): internal/cli (and api) lack a fail-safe TestMain
that redirects DaemonStateDir to a temp dir. Add internal/cli/main_test.go TestMain calling
SetDaemonStateRootForTest(tempdir) so NO cli test can touch the real %LOCALAPPDATA%\mcp-local-hub fleet.
Extend internal/api/main_test.go (currently only EnableSupervisorIPCTestPipeIsolation) to also redirect state.
VERIFY SAFELY: back up intent -> run full cli suite -> assert intent byte-identical (proves the override
covers all paths). Recoverable in 30s via mcphub restart if a path slips. Bundle into r35 or dedicated fix.
UNTIL FIXED: only ever run NARROW -run cli/api tests (never the broad suite) on this host.
