# Roadmap — finish Windows console opt-in delivery R2

Admission source: direct user instruction on 2026-08-11 to resume the parked delivery, finish the working no-console hub, then process new and previously merged pull requests lacking exact-head Codex PASS.

## Ordered outcome

1. Close or correctly disposition the broad CLI and GUI lifecycle gates without weakening the already-GREEN Windows console contract.
2. Execute real Linux and native macOS gates with durable terminal oracles.
3. Obtain independent final QA and publication-safety approval.
4. Commit and push only after the human publication gate, then install/restart the canonical hub and prove the normal path creates no visible consoles.
5. After hub delivery, inventory new pull requests and merged pull requests lacking exact-head Codex PASS; retrigger Codex after the refreshed quota and repair actionable current-head findings to PASS.

## Success signal

The installed canonical hub is functional, the fleet is restored, normal launches produce no visible console windows, `--debug-console` is the sole opt-in, required verification gates are green, and the queued pull-request review wave is reconciled against authoritative hosted state.
