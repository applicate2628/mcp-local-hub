# Phase 1 re-verification evidence map

## Files & symbols

| Finding surface | Owning files and symbols | Evidence classification |
|---|---|---|
| Route request entry points | `Service.RouteHandler` creates the route daemon multiplexer and mounts only `/serena/mcp` and `/lsp/`; `buildRouteServer` supplies the handlers and sets `ReadOnlyRouterMode: true` (`internal/gui/route_adapter.go:20-28`, `internal/cli/route.go:167-217`, `internal/cli/route.go:250-255`). | `static-read` |
| Serena route resolution | The Serena tool path calls the `WorkspaceResolver` interface through `ResolveByPath`; the route builder supplies `serena_routing.NewReadOnlyWorkspaceResolver` (`internal/gui/serena_router.go:713-745`, `internal/cli/route.go:184-192`). | `static-inference`; `ASSUMPTION (UNVERIFIED — dynamic callback binding; falsified by a named request test that proves which resolver method executes)` |
| Serena read-only registry refresh | `ResolveByPath` obtains a snapshot, the snapshot refreshes, and the read-only branch calls `Registry.Load` without `Registry.Lock` (`internal/api/serena_routing/resolver.go:107-114`, `internal/api/serena_routing/resolver.go:125-162`, `internal/api/serena_routing/resolver.go:242-256`). | `static-read` |
| LSP route resolution | The LSP tool path calls `resolveLSPToolWorkspace`, then the `WorkspaceResolver` interface through `ResolveByPath`; the route builder supplies `lsp_routing.NewReadOnlyWorkspaceResolver` (`internal/gui/lsp_router.go:328-339`, `internal/gui/lsp_router.go:440-482`, `internal/cli/route.go:203-215`). | `static-inference`; `ASSUMPTION (UNVERIFIED — dynamic callback binding; falsified by a named request test that proves which resolver method executes)` |
| LSP read-only registry refresh | `ResolveByPath` matches a registration through `snapshotRegistry`; the read-only refresh branch calls `Registry.Load` without `Registry.Lock` (`internal/api/lsp_routing/resolver.go:123-152`, `internal/api/lsp_routing/resolver.go:240-254`, `internal/api/lsp_routing/resolver.go:307-368`). | `static-read` |
| LSP trusted-root read | The read-only router wires `api.LSPWorkspaceRootTrusted`; an unregistered first touch invokes the trusted-root callback before it discovers that `AutoRegisterFn` is nil, and the callback loads the read-only trusted-roots file (`internal/gui/lsp_router.go:129-182`, `internal/gui/lsp_router.go:515-620`, `internal/api/lsp_trusted_roots.go:176-220`, `internal/api/lsp_trusted_roots.go:329-334`). | `static-inference`; `ASSUMPTION (UNVERIFIED — dynamic trusted-root callback; falsified by a named unregistered-route request test)` |
| Shared inode-anchored reader | `Registry.Load` and `LoadLSPTrustedRoots` both enter `readStateFileInodeAnchored` (`internal/api/workspace_registry.go:126-155`, `internal/api/lsp_trusted_roots.go:176-203`). | `static-read` |
| Hardcoded lower-layer writer | Both operating-system implementations emit `_ = LogHubMcpEvent(...)` when the parent state directory is broader than the hardened policy; that emission occurs before opening the requested registry/trusted-roots leaf (`internal/api/hub_mcp_state_read_inode_windows.go:91-161`, `internal/api/hub_mcp_state_read_inode_posix.go:42-118`). | `static-read` |
| Hub event persistence | `LogHubMcpEvent` marshals the event and enters the append path; the append path resolves/creates the daemon state directory, locks `hub-mcp.log.lock`, may rotate `hub-mcp.log.1`, and creates/appends/synchronizes `hub-mcp.log` (`internal/api/hub_mcp_log.go:98-116`, `internal/api/hub_mcp_log.go:237-309`, `internal/api/state_paths_prod.go:38-50`, `internal/api/state_paths_windows.go:56-66`, `internal/api/state_paths_unix.go:49-63`). | `static-read` |
| Direct router audit sinks | Both read-only router constructors leave durable auto-registration callbacks unset and supply `routeReadOnlySink`; that sink encodes one JSON record to process standard error (`internal/gui/serena_router.go:249-298`, `internal/gui/lsp_router.go:129-182`, `internal/gui/route_readonly_audit.go:35-60`). | `static-read` for construction and sink body; `static-inference`; `ASSUMPTION (UNVERIFIED — callback execution is dynamic)` for request-time dispatch |
| Route-daemon seeder | `runSupervise` adopts the return value of `ensureBuiltinRouteDaemonAtStartup`; that function resolves the strict front port, persists through `MutateSupervisorIntentIfChanged`, then conditionally reapplies the canonical row to caller memory (`internal/cli/supervise.go:933-944`, `internal/cli/supervise.go:2569-2714`, `internal/api/supervisor_intent_mutate.go:12-49`). | `static-read` |
| Canonical route descriptor | `EnsureBuiltinRouteDaemon` first protects foreign identities with `Server`/`Daemon` checks, then uses `reflect.DeepEqual` for the complete `SupervisorDaemon` value (`internal/api/builtin_route_daemon.go:128-158`). The descriptor fields are defined in `SupervisorDaemon` and its optional nested `DaemonRuntimeSpec` (`internal/api/supervisor_intent.go:62-151`). | `static-read` |
| Strict port resolver | The strict CLI wrapper calls `api.ResolveMCPFrontPort`; the API returns settings read, parse, and range errors instead of the graceful default used by the separate fallback function (`internal/cli/mcp_front_port.go:36-46`, `internal/api/mcp_front_port.go:39-64`). | `static-read` |
| Registry publication owner | `Registry.Save` serializes the complete registry and delegates to `WriteStateFileBytesLockHeld`; the secure writer publishes a complete temporary file by handle-relative atomic rename on POSIX and Windows (`internal/api/workspace_registry.go:158-189`, `internal/api/state_file_helper.go:107-185`, `internal/api/secure_write_client_config.go:68-89`, `internal/api/secure_write_posix.go:162-174`, `internal/api/secure_write_windows.go:285-301`). | `static-read` |

## Flows

### Request path to the hardcoded low-level event writer

The low-level `LogHubMcpEvent` path is statically request-reachable; it is not excluded by the read-only route construction.

1. Serena request chain: `RouteHandler` → `/serena/mcp` → `serenaRouterHandler` → dynamic `Resolver.ResolveByPath` → `NewReadOnlyWorkspaceResolver.ResolveByPath` → `snapshot` → `refresh` → `Registry.Load` → `readStateFileInodeAnchored` → hardcoded `LogHubMcpEvent` on a broadened existing parent (`internal/gui/route_adapter.go:20-28`, `internal/gui/serena_router.go:713-745`, `internal/cli/route.go:184-192`, `internal/api/serena_routing/resolver.go:107-162`, `internal/api/serena_routing/resolver.go:242-256`, `internal/api/workspace_registry.go:126-155`, `internal/api/hub_mcp_state_read_inode_windows.go:91-161`, `internal/api/hub_mcp_state_read_inode_posix.go:42-118`). This complete request chain is `static-inference`; `ASSUMPTION (UNVERIFIED — the interface dispatch was not observed at runtime in this session)`.
2. Registered LSP request chain: `RouteHandler` → `/lsp/` → `lspRouterHandler` → `resolveLSPToolWorkspace` → dynamic `Resolver.ResolveByPath` → read-only resolver snapshot/refresh → `Registry.Load` → the same inode reader and event writer (`internal/gui/route_adapter.go:20-28`, `internal/gui/lsp_router.go:328-339`, `internal/gui/lsp_router.go:440-482`, `internal/cli/route.go:203-215`, `internal/api/lsp_routing/resolver.go:123-152`, `internal/api/lsp_routing/resolver.go:240-254`, `internal/api/lsp_routing/resolver.go:307-368`). This complete request chain is `static-inference`; `ASSUMPTION (UNVERIFIED — the interface dispatch was not observed at runtime in this session)`.
3. Unregistered LSP first-touch chain: the read-only LSP router wires `api.LSPWorkspaceRootTrusted`; `workspaceFromResolvedLSPPath` performs that check before the nil `AutoRegisterFn` guard; the trusted-root loader enters the same inode reader and therefore the same hardcoded event writer (`internal/gui/lsp_router.go:129-182`, `internal/gui/lsp_router.go:515-620`, `internal/api/lsp_trusted_roots.go:176-220`, `internal/api/lsp_trusted_roots.go:329-334`). This complete request chain is `static-inference`; `ASSUMPTION (UNVERIFIED — the trusted-root callback was not observed at runtime in this session)`.

When the broadened-parent branch fires, the observable shared-state mutations are state-directory creation or permission normalization, `hub-mcp.log.lock` creation, `hub-mcp.log` creation/append/synchronization, and possible `hub-mcp.log.1` removal/rename during rotation (`internal/api/hub_mcp_log.go:237-309`, `internal/api/state_paths_prod.go:38-50`, `internal/api/state_paths_windows.go:56-66`, `internal/api/state_paths_unix.go:49-63`). This mutation inventory is `static-read`.

The direct router audit path is separate: read-only constructors provide `routeReadOnlySink`, so router-owned audit calls target standard error, not `LogHubMcpEvent` (`internal/gui/serena_router.go:249-298`, `internal/gui/lsp_router.go:129-182`, `internal/gui/route_readonly_audit.go:35-60`). This does not intercept or parameterize the lower-level inode-reader event call, which is hardcoded inside the shared reader (`internal/api/hub_mcp_state_read_inode_windows.go:91-143`, `internal/api/hub_mcp_state_read_inode_posix.go:42-107`). The separation is `static-read`.

Other route-time write-capable branches are guarded at the route-owned boundary:

- Serena activity persistence returns immediately in read-only router mode; the route builder sets that mode (`internal/gui/serena_idle_sweeper.go:103-136`, `internal/cli/route.go:167-173`). `static-read`.
- Serena auto-registration and wake-idle callbacks require nonnil injected functions, while `SetSerenaRouterReadOnly` leaves them unset (`internal/gui/serena_router.go:249-298`, `internal/gui/serena_router.go:846-872`, `internal/gui/serena_router.go:1384-1388`). `static-read` for guards and wiring; `static-inference`; `ASSUMPTION (UNVERIFIED — dynamic callbacks were not observed)` for runtime exclusion.
- LSP auto-registration requires a nonnil injected function, while `SetLSPRouterReadOnly` leaves it unset (`internal/gui/lsp_router.go:129-182`, `internal/gui/lsp_router.go:515-594`). `static-read` for guards and wiring; `static-inference`; `ASSUMPTION (UNVERIFIED — dynamic callbacks were not observed)` for runtime exclusion.

### Registry writers and atomicity

All located production registry writers converge on `Registry.Save`; no located writer truncates or rewrites `workspaces.yaml` in place. That negative conclusion is `ASSUMPTION (UNVERIFIED — bounded text search is not proof of absence)`. The located writer inventory is:

| Writer/caller family | Save or wrapper evidence | Lock/atomicity evidence |
|---|---|---|
| Registry methods | `PutLastToolsCallAt` and `PutLifecycleWithTimestamps` lock, load, mutate, and save; `PutLifecycle` delegates to the timestamped method (`internal/api/workspace_registry.go:423-496`). | `Registry.Save` delegates to the atomic secure writer (`internal/api/workspace_registry.go:158-189`, `internal/api/state_file_helper.go:107-185`). |
| Membership and pruning | Membership and prune operations call `reg.Save` (`internal/api/membership.go:80`, `internal/api/prune_workspace.go:159`). | Publication is full-file atomic rename through the common owner (`internal/api/secure_write_client_config.go:68-89`, `internal/api/secure_write_posix.go:162-174`, `internal/api/secure_write_windows.go:285-301`). |
| Registration | General registration calls `reg.Save` on its mutation paths (`internal/api/register.go:817-854`, `internal/api/register.go:1161`). | Same common atomic publication owner. |
| Supervisor registration | Supervisor registration calls `reg.Save` on its mutation paths (`internal/api/register_supervisor.go:306-331`). | Same common atomic publication owner. |
| Serena/LSP auto-registration | Serena and LSP auto-registration call `reg.Save` (`internal/api/serena_auto_register.go:266-288`, `internal/api/lsp_auto_register.go:219-306`). | Same common atomic publication owner. |
| Dynamic-pool reallocation | Reallocation calls `reg.Save` on both located save paths (`internal/api/reallocate_dynamic_pool.go:98-150`). | Same common atomic publication owner. |
| Lazy proxy | The lazy proxy calls `reg.Save` directly and also uses lifecycle/activity registry wrappers (`internal/daemon/lazy_proxy.go:1710`, `internal/daemon/lazy_proxy.go:423-423`, `internal/daemon/lazy_proxy.go:1351-1405`, `internal/daemon/lazy_proxy.go:1576-1647`, `internal/daemon/lazy_proxy.go:1832-2089`). | Direct and wrapped publication converge on `Registry.Save`. |
| CLI workspace operations | Workspace commands call `reg.Save` (`internal/cli/workspace_cmd.go:289-479`). | Same common atomic publication owner. |
| CLI daemon workspace | Daemon workspace mutation calls `reg.Save` (`internal/cli/daemon_workspace.go:221`). | Same common atomic publication owner. |
| Serena migration | Located migration paths call `reg.Save` (`internal/cli/migrate_serena.go:629-629`, `internal/cli/migrate_serena.go:1580-1727`). | Same common atomic publication owner. |

`Registry.Load` itself is an unlocked reader, and the production mutation methods place locking outside `Save`; atomic rename protects readers from partial publication, while caller locking protects writer coordination (`internal/api/workspace_registry.go:126-189`, `internal/api/workspace_registry.go:423-496`). This distinction is `static-read`. The read-only route resolvers intentionally omit writer locking while retaining atomic-read safety (`internal/api/serena_routing/resolver.go:125-188`, `internal/api/lsp_routing/resolver.go:319-368`).

### Seeder return paths and ordering

`runSupervise` replaces its local intent with the seeder return value, so return-value mutation is caller-visible (`internal/cli/supervise.go:933-944`). The complete bounded return-path inventory is:

| Path | Persistent effect | Returned/caller intent | Event ordering | Evidence |
|---|---|---|---|---|
| Test-disable branch | None | Original input | No event | `internal/cli/supervise.go:2569-2572` (`static-read`) |
| Empty canonical executable | None | Original input | Failure event before return | `internal/cli/supervise.go:2575-2586` (`static-read`) |
| Strict-port failure with existing nonnil intent | None; persistence is not reached | Existing input remains unchanged | Failure event before return | `internal/cli/supervise.go:2588-2625` (`static-read`) |
| Strict-port failure with nil intent | None; persistence is not reached | A newly allocated empty intent is returned because allocation precedes port resolution | Failure event before return | `internal/cli/supervise.go:2588-2625` (`static-read`) |
| Persistence failure with existing nonnil intent | Mutation callback may have prepared a candidate, but `MutateSupervisorIntentIfChanged` does not publish success | Existing caller object is not reapplied | Failure event after persistence returns error | `internal/cli/supervise.go:2647-2671`, `internal/api/supervisor_intent_mutate.go:12-49` (`static-read`) |
| Persistence failure with nil intent | Same persistence failure | A newly allocated empty intent is returned; allocation occurred before persistence | Failure event after persistence returns error | `internal/cli/supervise.go:2588-2590`, `internal/cli/supervise.go:2647-2671` (`static-read`) |
| Persistence success, `changed == false` | No new write required | Caller object is returned without canonical-row reapplication | Success event after persistence result | `internal/cli/supervise.go:2647-2655`, `internal/cli/supervise.go:2674-2713` (`static-read`); caller/disk equivalence under concurrent mutation is `ASSUMPTION (UNVERIFIED — falsified by a controlled changed-false stale-caller probe)` |
| Persistence success, `changed == true`, reapply failure | Canonical row is already persisted | Caller object is returned without the failed reapplication | Failure event after persistence; no success event | `internal/cli/supervise.go:2674-2701` (`static-read`) |
| Persistence success, `changed == true`, reapply success | Canonical row is persisted | Canonical row is applied to caller memory | Success event after persistence and reapplication | `internal/cli/supervise.go:2674-2713` (`static-read`) |

The failure invariant is therefore split: an existing intent row is preserved on strict-port and persistence failures, but a fresh nil intent is changed to a nonnil empty intent before either failure is known (`internal/cli/supervise.go:2588-2625`, `internal/cli/supervise.go:2647-2671`). This is `static-read`. Success emission is ordered after the persistence result and, when a change was published, after caller-memory reapplication (`internal/cli/supervise.go:2647-2713`). This is `static-read`; actual callback timing was not observed in this session.

## Contracts

The admitted decision says the front route daemon reads registry and supervisor-intent state without becoming their writer and must not own supervisor cutover (`work-items/decisions/2026-07-25-mcp-data-plane-off-gui-onto-supervised-front-daemon.md:69-79`). The work-item invariant additionally requires request paths to avoid shared writes, unlocked readers to rely only on atomic publication, and failed persistence to avoid caller-intent mutation or success emission (`work-items/active/2026-07-25-mcp-front-daemon/brief.md:49-54`).

| Finding | Re-verification disposition | Factual basis |
|---|---|---|
| P1-1: route request path performs zero shared writes | `REVISE` — not closed | Direct router audit callbacks target standard error, but the registry and trusted-root read paths retain a hardcoded request-reachable `LogHubMcpEvent` branch on broadened parents (`internal/gui/route_readonly_audit.go:35-60`, `internal/api/hub_mcp_state_read_inode_windows.go:91-161`, `internal/api/hub_mcp_state_read_inode_posix.go:42-118`). End-to-end reachability across injected callbacks is `static-inference`; `ASSUMPTION (UNVERIFIED — runtime route probe not run)`. |
| P1-2: persist → adopt → emit ordering and failure preservation | `REVISE` — partially closed | Success ordering is persistence then optional in-memory adoption then success event; existing-row failures preserve the caller object. Fresh nil input is allocated before strict-port resolution/persistence and returns nonnil empty on failure (`internal/cli/supervise.go:2588-2713`). |
| P2-4: descriptor collision and full canonical equality | `static source closure; runtime unverified` | Foreign `Server`/`Daemon` identities are rejected before full `reflect.DeepEqual`; every exported field in `SupervisorDaemon`, including optional `RuntimeSpec`, participates in equality (`internal/api/builtin_route_daemon.go:128-158`, `internal/api/supervisor_intent.go:62-151`). |
| P2-5: strict port resolution | `REVISE` — partially closed | The strict resolver returns settings errors and the seeder stops before persistence; existing-row preservation is covered statically and by a named test, while fresh nil intent still changes shape and lacks a named seeder test (`internal/api/mcp_front_port.go:39-64`, `internal/cli/supervise.go:2588-2625`, `internal/cli/builtin_route_daemon_test.go:134-200`). |
| P2-3: read-only resolver locks and trusted-root directory creation | `REVISE` — direct mechanisms closed, shared-read side effect remains | Read-only resolvers omit registry lock acquisition and the read-only trusted-root path does not create the state directory by itself (`internal/api/serena_routing/resolver.go:125-188`, `internal/api/lsp_routing/resolver.go:319-368`, `internal/api/lsp_trusted_roots.go:90-121`). The shared inode reader can nevertheless write the hub event log on a broadened existing parent (`internal/api/hub_mcp_state_read_inode_windows.go:91-161`, `internal/api/hub_mcp_state_read_inode_posix.go:42-118`). |
| P2-6: regression tests falsify all corrected contracts | `REVISE` — not closed | The named tests cover direct sink wiring, hardened-parent no-write requests, resolver lock controls, existing-row failures, and descriptor cases, but omit broadened-parent request reads and fresh nil seeder failures (`internal/cli/route_i6_readonly_test.go:139-224`, `internal/gui/route_readonly_test.go:120-223`, `internal/cli/builtin_route_daemon_test.go:25-200`). |
| P3-7: no trailing whitespace | `PASS` for the current diff | `git diff --check origin/master..HEAD` returned exit code 0 in this session. `runtime-verified` command evidence; no build or test process was run. |

Descriptor equality has these exact semantics:

- The canonical descriptor initializes `TaskName`, `Server`, `Daemon`, `Command`, `Args`, `Env`, `Workspace`, `Port`, `ManifestHash`, `StartupBindDeadlineSeconds`, and `RuntimeSpec` through the complete `SupervisorDaemon` value (`internal/api/builtin_route_daemon.go:55-72`, `internal/api/supervisor_intent.go:62-110`). `static-read`.
- `SupervisorDaemon` and nested `DaemonRuntimeSpec` contain no time values and no unexported fields; all declared fields are visible to `reflect.DeepEqual` (`internal/api/supervisor_intent.go:62-151`). `static-read`.
- `reflect.DeepEqual` distinguishes nil slices/maps/pointers from empty nonnil values. Consequently, an empty `Env` map, empty `Args` slice, or allocated empty `RuntimeSpec` is noncanonical when the canonical value is nil (`internal/api/builtin_route_daemon.go:55-72`, `internal/api/builtin_route_daemon.go:154-158`). This semantic statement is `static-inference`; `ASSUMPTION (UNVERIFIED — falsified by explicit nil-versus-empty table cases)`.
- Because these fields use JSON `omitempty`, a canonical nil value remains absent after serialization; the expectation that an empty-to-nil normalization produces one rewrite rather than repeated churn is `static-inference`; `ASSUMPTION (UNVERIFIED — falsified by ensure → serialize → reload → ensure test)` (`internal/api/supervisor_intent.go:62-110`, `internal/api/builtin_route_daemon.go:154-158`).

Strict-port failure has two distinct caller states:

- Existing intent: the function returns the same existing object before any persistence callback is invoked (`internal/cli/supervise.go:2588-2625`). `static-read`.
- Fresh host with nil intent: the function allocates an empty intent before strict resolution and returns that nonnil object on failure; no route row is inserted and no persistence callback is reached (`internal/cli/supervise.go:2588-2625`). `static-read`.

## Tests & coverage

No test, build, daemon, or fleet process was executed in this research session. Every test result below is therefore a source-level coverage statement, not a runtime pass.

| Contract or control | Named test/source coverage | Coverage limit |
|---|---|---|
| Serena read-only resolver does not wait on an exclusive registry lock | `TestNewReadOnlyWorkspaceResolver_NeverBlocksOnConcurrentExclusiveLock`; production-lock control `TestNewWorkspaceResolver_BlocksOnConcurrentExclusiveLock` (`internal/api/serena_routing/resolver_test.go:570-685`). | `static-read`; named tests not run in this session. |
| LSP read-only resolver does not wait on an exclusive registry lock | Read-only and production-lock control pair (`internal/api/lsp_routing/resolver_test.go:305-413`). | `static-read`; named tests not run in this session. |
| Read-only trusted-root path does not create a missing state directory | `TestLoadDefaultLSPTrustedRoots_DoesNotCreateStateDirectory` (`internal/api/lsp_trusted_roots_test.go:60-91`). | It does not exercise an existing broadened parent, the condition that enters the hardcoded event writer. |
| Route daemon registered-workspace failure does not change its hardened state tree | `TestBuildRouteServer_RegisteredWorkspaceUnreachableBackend_NoSharedStateFileWrite` (`internal/cli/route_i6_readonly_test.go:139-224`). | The test hardens its temporary state directory before the request (`internal/cli/route_i6_readonly_test.go:154-158`), excluding the broadened-parent event branch. |
| Serena read-only route failure does not change its hardened state tree | The companion no-write request test explicitly hardens the parent before exercising the request (`internal/gui/route_readonly_test.go:120-223`). | The setup excludes the broadened-parent event branch it would need to falsify. |
| LSP detached notification uses an injected audit function and read-only wiring supplies `routeReadOnlySink` | `TestForwardLSPNotificationDetached_UsesProvidedAuditFnNotHardcodedLogHubMcpEvent` and `TestSetLSPRouterReadOnly_WiresRouteReadOnlySinkAsAuditFn` (`internal/gui/lsp_router_test.go:1455-1523`). | These tests cover router-owned audit dispatch, not the lower-level hardcoded event inside state reads. |
| Foreign descriptor collision and identity drift | `TestEnsureBuiltinRouteDaemon_ForeignRowCollisionRejectedLoudly` and `TestEnsureBuiltinRouteDaemon_ServerDaemonDriftIsNotSilentlyAcceptedAsCanonical` (`internal/api/builtin_route_daemon_test.go:101-175`). | Named tests not run in this session. |
| Complete descriptor comparison and duplicate-key collapse | `TestEnsureBuiltinRouteDaemon_OwnRowFullCanonicalCompare` and `TestEnsureBuiltinRouteDaemon_DuplicateCanonicalKeyRowsCollapseToOne` (`internal/api/builtin_route_daemon_test.go:177-249`). | The full-compare test names command drift but has no explicit cases for nil versus empty `Args`/`Env`/`RuntimeSpec`, `Workspace`, `ManifestHash`, or deadline fields. |
| Seeder persistence failure preserves an existing row | `TestEnsureBuiltinRouteDaemonAtStartup_PersistFailurePreservesExistingRow` (`internal/cli/builtin_route_daemon_test.go:79-132`). | Input is an existing nonnil intent (`internal/cli/builtin_route_daemon_test.go:109-112`); nil fresh-host input is absent. |
| Seeder strict-port failure preserves an existing row | `TestEnsureBuiltinRouteDaemonAtStartup_PortResolutionFailurePreservesExistingRow` (`internal/cli/builtin_route_daemon_test.go:134-200`). | Input is an existing row (`internal/cli/builtin_route_daemon_test.go:169-173`); nil fresh-host input is absent. |
| Seeder success from an empty intent | Success test at `internal/cli/builtin_route_daemon_test.go:25-77`. | The nominally fresh case supplies a nonnil empty intent (`internal/cli/builtin_route_daemon_test.go:51-57`), so it does not cover nil-input allocation or failure. |
| Strict API rejects corrupt settings | `TestResolveMCPFrontPort_ErrorsOnCorruptSettingsFile` (`internal/api/mcp_front_port_test.go:99-118`). | It tests the resolver, not the seeder's fresh-host return value. |
| Trailing whitespace | Current-session `git diff --check origin/master..HEAD` exit code 0. | `runtime-verified`; this is a Git diff check, not a product test. |

The exact missing falsifiers are bounded: a broadened-parent request test for each dynamic state-read route class, a nil-input strict-port failure seeder test, a nil-input persistence failure seeder test, and descriptor table cases covering nil/empty and serialize-reload stability. This inventory is `static-read` from the gaps above; the absence claim remains `ASSUMPTION (UNVERIFIED — bounded text search and focal test reads do not prove no equivalent test exists elsewhere)`.

## Similar implementations

- Production and read-only resolver variants share the same snapshot/matching logic; their explicit divergence is lock acquisition. Production branches call `Registry.Lock`, while read-only branches call `Registry.Load` directly (`internal/api/serena_routing/resolver.go:125-188`, `internal/api/lsp_routing/resolver.go:319-368`). `static-read`.
- Trusted-root paths have distinct read-only and write-capable path resolvers. The read-only path uses `DaemonStateDirReadOnly`, while blessing/removal operations own write-side state changes (`internal/api/lsp_trusted_roots.go:90-121`, `internal/api/lsp_trusted_roots.go:394-439`, `internal/api/lsp_trusted_roots.go:493-547`). `static-read`.
- Registry mutations converge on the complete-file `Registry.Save` owner and secure atomic publication rather than per-call direct file writes (`internal/api/workspace_registry.go:158-189`, `internal/api/state_file_helper.go:107-185`, `internal/api/secure_write_client_config.go:68-89`). `static-read`.
- Route-owned audit indirection exists in both routers and converges on `routeReadOnlySink`; the shared inode reader does not use that indirection and instead names `LogHubMcpEvent` directly (`internal/gui/serena_router.go:249-298`, `internal/gui/lsp_router.go:129-182`, `internal/gui/route_readonly_audit.go:35-60`, `internal/api/hub_mcp_state_read_inode_windows.go:91-143`, `internal/api/hub_mcp_state_read_inode_posix.go:42-107`). `static-read`.

## Constraints

- This phase is evidence-only: product/test files, live fleet state, and real daemon state are protected; no product edits, CodeGraph calls, tests, builds, daemon starts, or fleet processes were admitted (`work-items/active/2026-07-25-mcp-front-daemon/roadmap.md:13-20`, `work-items/active/2026-07-25-mcp-front-daemon/plan.md:3-28`).
- Commits `3f72365d` and `d6c0501f` were treated as untrusted claims rather than accepted closure evidence, as required by the brief (`work-items/active/2026-07-25-mcp-front-daemon/brief.md:3-10`).
- Dynamic resolver and audit callback reachability is classified as `static-inference` unless a named test confirms the dispatch at runtime. No named test was run in this session.
- The current-session runtime evidence is limited to read-only Git inspection; only `git diff --check origin/master..HEAD` is used as a `runtime-verified` acceptance fact.

## Change risks

| Risk | Evidence |
|---|---|
| Read-only naming can conceal inherited lower-layer mutation | The read-only resolvers skip registry locks, yet their shared read primitive can emit a persistent event before it opens the requested leaf (`internal/api/serena_routing/resolver.go:125-162`, `internal/api/lsp_routing/resolver.go:319-368`, `internal/api/hub_mcp_state_read_inode_windows.go:91-161`, `internal/api/hub_mcp_state_read_inode_posix.go:42-118`). `static-read`. |
| Hardened-parent tests mask the remaining branch | Both request no-write tests harden their temporary parent, while the lower-level event branch requires a broadened parent (`internal/cli/route_i6_readonly_test.go:154-158`, `internal/gui/route_readonly_test.go:120-154`, `internal/api/hub_mcp_state_read_inode_windows.go:91-143`, `internal/api/hub_mcp_state_read_inode_posix.go:42-107`). `static-read`. |
| Fresh-host failure mutates caller-visible shape | The seeder allocates before strict resolution and persistence, and `runSupervise` adopts the returned object (`internal/cli/supervise.go:933-944`, `internal/cli/supervise.go:2588-2625`, `internal/cli/supervise.go:2647-2671`). `static-read`. |
| Changed-false persistence can leave caller/disk equivalence implicit | In-memory reapplication occurs only when `changed` is true (`internal/cli/supervise.go:2647-2689`). Equivalence when concurrent disk state changes is `ASSUMPTION (UNVERIFIED — controlled stale-caller probe not run)`. |
| Nil/empty equality can create normalization churn if serialization semantics differ from the static expectation | Full `reflect.DeepEqual` includes nil/empty distinctions, while descriptor fields use `omitempty` (`internal/api/builtin_route_daemon.go:154-158`, `internal/api/supervisor_intent.go:62-151`). One-rewrite stability is `ASSUMPTION (UNVERIFIED — serialize-reload stability test not located in the focal file)`. |
| Fix-over-fix history reduces commit-message evidentiary value | Git history inspected in this session shows the seeder introduced before `3f72365d`, the hardcoded read-event path predating the branch, and `d6c0501f` correcting trailing whitespace after `3f72365d` claimed the diff clean. This is current-session Git command evidence; source behavior is independently anchored above. |

## Unresolved questions

| Question | Current classification | Exact falsifier |
|---|---|---|
| Does a live Serena route request execute the statically wired read-only resolver and enter the broadened-parent event branch? | `ASSUMPTION (UNVERIFIED)` because interface dispatch is dynamic. | A named Serena route request test with an existing broadened state parent and before/after complete state-tree snapshot. |
| Does a registered LSP route request execute the statically wired read-only resolver and enter the broadened-parent event branch? | `ASSUMPTION (UNVERIFIED)` because interface dispatch is dynamic. | A named registered-LSP route request test with the same broadened-parent snapshot. |
| Does unregistered LSP first touch execute the trusted-root callback before rejecting nil auto-registration? | `ASSUMPTION (UNVERIFIED)` because callback dispatch is dynamic. | A named unregistered-LSP request test with callback observation and broadened-parent snapshot. |
| Is every production registry writer represented in the located `Registry.Save` inventory? | `ASSUMPTION (UNVERIFIED)`; two-angle text search found no direct alternate writer, but absence is not proven. | Repository-wide syntax-aware write-call inventory for the canonical registry path plus an observed write trace. |
| Can `changed == false` coincide with a caller object that differs from the disk value after concurrent mutation? | `ASSUMPTION (UNVERIFIED)`. | Controlled stale-caller/concurrent-disk mutation around `MutateSupervisorIntentIfChanged`. |
| Does descriptor normalization converge after one persist/reload cycle for every nil/empty field? | `ASSUMPTION (UNVERIFIED)`. | Table-driven ensure → serialize → reload → ensure equality test covering `Args`, `Env`, and `RuntimeSpec`. |

## Research admission gates

| Gate | Evidence state | Verdict |
|---|---|---|
| 1. Regression-risk localization | Exact request, registry, seeder, descriptor, port, and whitespace owners plus adjacent controls are identified with file/line anchors. Dynamic callback execution and runtime behavior were not observed, and each such limit has an exact bounded falsifier. | `PASS` for research completeness |
| 2. Metric alignment | The acceptance metric is zero shared filesystem change across the complete state tree (`work-items/active/2026-07-25-mcp-front-daemon/brief.md:49-54`, `work-items/active/2026-07-25-mcp-front-daemon/plan.md:30-80`). Existing no-write tests harden the parent and therefore exclude the remaining broadened-parent writer branch (`internal/cli/route_i6_readonly_test.go:154-158`, `internal/gui/route_readonly_test.go:120-154`). The mismatch is localized to an exact condition and test boundary. | `PASS` for research completeness; product closure remains `REVISE` |
| 3. Known-limit disclosure | Static-only scope, dynamic callback uncertainty, broadened-parent behavior, fresh nil seeder input, nil/empty descriptor semantics, and changed-false caller/disk equivalence are explicit above. | `PASS` |
| 4. Bounded falsifiability | QA can target the named focal filters already listed in the plan and the four exact missing falsifier classes without broad repository discovery (`work-items/active/2026-07-25-mcp-front-daemon/plan.md:30-80`). The current evidence already gives exact request chains, conditions, state files, return paths, and success/failure thresholds. | `PASS` for research completeness; product closure remains `REVISE` |

**Research gate: `PASS`.** The analyst artifact is evidence-complete enough for QA to falsify the open claims without reopening broad discovery. **Product closure remains `REVISE`:** the lower-level hardcoded hub-event writer remains statically request-reachable, fresh nil seeder failures return a changed caller-visible object, and the tests do not cover those cases.

## Adjacent findings

The lower-layer request-read side effect is already recorded as the adjacent bug `2026-07-26-route-daemon-state-read-unhardened-parent-fallback-writes-hub-mcp-log`; that record names the same registry/trusted-root reader and log persistence path (`work-items/bugs/2026-07-26-route-daemon-state-read-unhardened-parent-fallback-writes-hub-mcp-log.md:12-40`). Its separation rationale states that the helper is a wider shared API boundary rather than a route-local implementation detail (`work-items/bugs/2026-07-26-route-daemon-state-read-unhardened-parent-fallback-writes-hub-mcp-log.md:42-67`). `static-read`.

No additional adjacent defect was promoted from bounded static evidence. The possible `changed == false` caller/disk divergence and registry-writer completeness remain explicit `ASSUMPTION (UNVERIFIED)` entries rather than findings.

## Searched and excluded

- Git scope and history: inspected `origin/master..HEAD`, focal file history, and blame for route adapter, registry reader/writer, seeder, and trailing-whitespace commits. Commit claims were not used as correctness evidence.
- Request surfaces: inspected the two mounts in `RouteHandler`, Serena and LSP tool workspace resolution, read-only resolver construction, trusted-root first touch, activity persistence, auto-registration, wake-idle, and injected audit sinks (`internal/gui/route_adapter.go:20-28`, `internal/gui/serena_router.go:249-298`, `internal/gui/lsp_router.go:129-182`).
- Registry alternate-writer search angle 1: searched non-test production code for the literal registry filename near direct `WriteFile`, `OpenFile`, `Create`, `Rename`, and state-file writer calls; no alternate direct writer was located. This negative result is `ASSUMPTION (UNVERIFIED)`.
- Registry alternate-writer search angle 2: searched production code for registry-path variables near the same write primitives and enumerated located `reg.Save`/`r.Save` call sites; all located calls converge on `Registry.Save`. This negative result is `ASSUMPTION (UNVERIFIED)`.
- Tests: inspected changed focal tests and their production controls, including resolver lock pairs, trusted-root directory behavior, actual route request tests, audit injection, descriptor collision/equality, strict-port rejection, and seeder failure preservation. No test was executed.
- Excluded by scope: CodeGraph, builds, test processes, daemon/fleet processes, live state directories, network probes, and product/test edits (`work-items/active/2026-07-25-mcp-front-daemon/roadmap.md:13-20`).
