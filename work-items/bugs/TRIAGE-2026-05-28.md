# Stale-bug triage — 2026-05-28 post-PR-#241

Performed per consultant amendment in `.scratch/reviews/fix-design-2026-05-28-next-direction-after-pr241.md`.
Master HEAD at triage time: `9828bf7` (PR #241 per-task Job Object merged).

This is **accounting closure**, not **implementation closure** — per user
directive "нужно все закрывать" interpreted via consultant memo: mark
every open bug as one of five categories so the backlog ceases to be
ambiguous. Fixes happen through normal sequencing (PR #242 →
job_protection surface, etc.) — they are not part of this triage step.

## Classification key

- **close** — bug status already marked `fixed` upstream; move to `closed/`.
- **superseded-by-phase-15** — CI-only concern that the planned
  Phase 15 (`docs/superpowers/plans/2026-05-16-v0.5.0-supervisor.md`
  Cross-platform CI rewrite) will wholesale replace. Do not fix
  separately.
- **needs-repro** — pre-v0.5.x bug that may have been silently fixed
  by subsequent refactors. Reproduce against `9828bf7` before any
  work; if fix is verified, move to `closed/`.
- **still-relevant-low** — confirmed open, low severity; eligible for
  cleanup PRs but not next-PR priority.
- **still-relevant-P2** — confirmed open, P2 severity; operator-visible
  gap. Schedule into post-Phase-9 sequence (PR #246+).
- **still-relevant-medium** — confirmed open, medium severity; between
  low and P2.

## Verdicts

| # | File | Verdict |
|---|---|---|
| 1 | `2026-05-08-api-tests-flock-contention-with-user-binary.md` | needs-repro |
| 2 | `2026-05-08-ci-bundle-freshness-gate.md` | superseded-by-phase-15 |
| 3 | `2026-05-08-ci-missing-tagged-test-job-and-symbol-assertion.md` | superseded-by-phase-15 |
| 4 | `2026-05-08-g3-error-text-redaction.md` | still-relevant-low |
| 5 | `2026-05-08-g3-populated-e2e-coverage.md` | still-relevant-low |
| ~~6~~ | ~~`2026-05-08-test-portinuse-flake-install-audit-failclosed.md`~~ | **CLOSED-2026-06-14** (the residual supervisor-host risk this DUPLICATE-of-#10 row flagged is FIXED — `internal/api/install.go:1760-1797` checks supervisor-intent BEFORE the scheduler-task fallback, so a v0.5.0 supervisor child IS now recognised as the legitimate port owner; adversarially re-verified, moved to `closed/`. Row #10 `install-test-port-9128-collision` STAYS OPEN — it tracks the DIFFERENT residual, the audit-before-port-check Option-B reorder. See Rows 6 + 10 note below) |
| 7 | ~~`2026-05-12-api-surfaces-status-restart-cleanup-race.md`~~ | **CLOSED-2026-06-14** (fn-pointer snapshotted into a local before the ctx-watch goroutine spawn at `api_surfaces.go:119,160`; race gone; adversarially re-verified, moved to `closed/`) |
| 8 | `2026-05-12-g4-phase1-optional-cleanups-deferred.md` | still-relevant-low |
| ~~9~~ | ~~`2026-05-12-health-go-sse-parser-incomplete.md`~~ | **close** (fixed by PR #263 `b6b90dcc` — shared `internal/api/sse.go` `extractSSEPayload` routing through event-aware `readSSEResponse`; exactly the shared-helper "Ideal" the doc proposed, plus event-boundary awareness for bot #263 P2; verified 2026-06-03 via `TestExtractSSEPayload`; moved to `closed/`. The needs-repro flag predated the #263 fix) |
| 10 | `2026-05-12-install-test-port-9128-collision.md` | still-relevant-low (DUPLICATE of #6 — same port-9128 defect; root cause fixed but residual supervisor-host risk, see Rows 6 + 10 note below) |
| ~~11~~ | ~~`2026-05-12-internal-api-suite-hangs-on-windows.md`~~ | **close** (moved to `closed/` 2026-05-28; status:fixed was already set in frontmatter) |
| ~~12~~ | ~~`2026-05-12-tool-catalog-gopls-mcp-hang.md`~~ | **close** (already fixed in commit `d27c10e` — `captureToolsList` got a 20 s `context.WithTimeout` + subprocess kill; verified 2026-06-02; moved to `closed/`. Was needs-repro; the wall-clock cap this doc asked for is present at HEAD) |
| 13 | `2026-05-15-demigrate-fallback-when-no-pre-hub-form.md` | still-relevant-P2 (PARTIALLY FIXED — PR #257 `f144dea` fixed the originally-reported codename-rename case, but the broad "должно работать всегда" mandate is NOT fully met: a pre-marker hub-managed entry with all backups pruned still fails closed by design, per the #218 anti-deletion safety. Re-opened 2026-06-02 after the codex bot flagged the over-claim on PR #259 — see the doc Status for the accept-vs-fix decision) |
| ~~14~~ | ~~`2026-05-19-codex-config-symlink-blocked-by-pr209.md`~~ | **close** (fixed by PR #217 `9e89abe` "Fix 1" + refined by PR #258 `44512e2` — `MCPHUB_ALLOW_CLIENT_CONFIG_SYMLINK` opt-in + distinct `error-symlink` scan category; verified 2026-06-02; moved to `closed/`. The "still-relevant-P2" flag was a pr-review pass reading the stale doc, not HEAD) |
| ~~15~~ | ~~`2026-05-19-state-file-verify-rejects-write-broadened-parent-dacl.md`~~ | **close** (already fixed by PR #217 `9e89abe`, inode-anchored read — verified 2026-06-02; moved to `closed/`. The "still-relevant-P2" flag was a pr-review pass reading the stale doc, not HEAD; fix shipped 61 min after filing) |
| 16 | `2026-05-20-tests-leak-state-into-production-logs.md` | still-relevant-medium (PARTIALLY FIXED — PR #264 `914d0cf` fixed the api-side state-file-helper leak via `isolateStateDir` + ambient-env clearing, but the gui-side `TestRealClientInitializer_HappyPath` (internal/gui, untouched by #264) audit-log leak REMAINS — it isolates `LOCALAPPDATA` but not `daemonStateRootOverride`, so the client-write audit event still leaks to the real `hub-mcp.log`. RE-OPENED 2026-06-03 after the codex bot flagged the over-claim on #265 r6) |
| 17 | `a3a-vault-concurrent-edit-lww.md` | needs-repro (Apr 2026, pre-v0.5.x supervisor) |
| 18 | `b1-backup-file-race.md` | needs-repro (Apr 2026, pre-v0.5.x supervisor; secure-write hardening may have addressed) |

## Counts

Counts below are recomputed from the verdict table itself (the canonical
source). Note: the original 2026-05-28 prose counts (still-relevant 11 /
needs-repro 4) did not match the original table, which actually held 10
still-relevant + 5 needs-repro rows (1+2+10+5 = 18 total). The 2026-06-02
figures are table-accurate.

- **close**: 7 as of 2026-06-14 — rows 11, 12, 14, 15 (closed at/by
  2026-06-02), row 9 (closed 2026-06-03 by PR #263), plus rows 6 + 7 (CLOSED
  2026-06-14 — see the 2026-06-14 note below; both adversarially re-verified
  fully fixed and moved to `closed/`). Original prose said 1. (Rows 13 + 16 were
  briefly closed but RE-OPENED — row 13 on 2026-06-02, row 16 on 2026-06-03 as
  partially fixed; see their rows + notes.)
- **superseded-by-phase-15**: 2 — rows 2, 3.
- **still-relevant** (any severity): 6 as of 2026-06-14 — rows 4, 5, 8, 10,
  13, 16 (rows 6 + 7 left the bucket via close 2026-06-14; rows 13 + 16 are
  PARTIALLY FIXED but stay here — 13's broad mandate isn't fully met, 16's
  gui-side leak remains after #264; rows 14 + 15 left earlier via close).
  Original prose said 11; original table actually held 10.
- **needs-repro**: 3 as of 2026-06-03 — rows 1, 17, 18 (row 9 left via
  close 2026-06-03; row 12 left via close 2026-06-02). Original prose said
  4 but original table held 5 (1, 9, 12, 17, 18).

> **Reconciliation 2026-06-14:** rows **6** and **7** CLOSED by the
> adversarially-verified-fixed doc-hygiene batch (branch
> `chore/close-verified-fixed-bugs`). Row 6
> (`test-portinuse-flake-install-audit-failclosed`) — the supervisor-child
> port-ownership residual is fixed at `install.go:1760-1797`
> (supervisor-intent checked before the scheduler-task fallback); row 10
> (`install-test-port-9128-collision`) STAYS OPEN tracking the separate
> audit-before-port-check Option-B reorder (see the Rows 6 + 10 note). Row 7
> (`api-surfaces-status-restart-cleanup-race`) — fn-pointer snapshotted into a
> local before the ctx-watch goroutine spawn (`api_surfaces.go:119,160`), race
> gone. Both moved to `closed/`; verified against HEAD source + named tests, not
> a stale Status line.
>
> **Reconciliation 2026-06-03:** row **9** closed + row **16** PARTIALLY
> FIXED by the post-merge doc-hygiene batch after PRs #263 and #264 landed.
> Row 9 (`health-go-sse-parser-incomplete`, was needs-repro) — PR #263
> (`b6b90dcc`) shipped the shared `internal/api/sse.go` helper the doc itself
> proposed; fully closed. Row 16 (`tests-leak-state-into-production-logs`) —
> PR #264 (`914d0cf`) fixed the api-side leak (`isolateStateDir` + env
> clearing) but the gui-side `TestRealClientInitializer_HappyPath` audit-log
> leak (internal/gui, untouched by #264) remains; it was briefly closed then
> RE-OPENED 2026-06-03 when the codex bot caught the over-claim on #265 r6.
> Verified against the merged diffs + tests, not a stale Status line. PR #264's same "env" fix also closed
> `2026-05-29-api-symlink-dacl-tests-fail-on-broadened-parent-host` (not a
> 2026-05-28 TRIAGE row; surfaced by codex bot on #265 r1, moved to
> `closed/`). The sibling `api-tests-flock-contention` bug (a DIFFERENT
> defect — flock contention on the real lock, not the env leak) was
> explicitly NOT closed (untouched by #264). A new bug
> `2026-06-03-cli-supervise-statedir-override-ungated-in-production` was
> filed in the same batch (the cli-layer `MCPHUB_STATE_DIR_OVERRIDE` seam
> ships ungated in release binaries).
>
> **Reconciliation 2026-06-02:** row 15
> (`2026-05-19-state-file-verify-rejects-write-broadened-parent-dacl.md`)
> was found already-fixed by PR #217 (`9e89abe`, inode-anchored read) and
> moved to `closed/`. This was a stale flag — the original pr-review pass
> read the bug doc's `Status: OPEN`, not HEAD.
>
> The same HEAD-vs-doc re-verification then looked at **rows 12, 13, and
> 14**. Rows 12 and 14 were confirmed already-fixed phantoms and moved to
> `closed/`; row 13 was initially closed in the same pass but **RE-OPENED**
> after the codex bot caught that its fix is only partial (see its bullet):
>
> - Row 12 (`2026-05-12-tool-catalog-gopls-mcp-hang.md`, was needs-repro)
>   — fixed in commit `d27c10e`: `captureToolsList` now wraps the
>   `gopls mcp` probe in a 20 s `context.WithTimeout` + `cmd.Process.Kill()`
>   on exit (`internal/api/tool_catalog_test.go:188-214`), the exact
>   wall-clock cap the doc asked for; the 5-minute hang is gone.
> - Row 13 (`2026-05-15-demigrate-fallback-when-no-pre-hub-form.md`, was
>   still-relevant-P2) — **PARTIALLY fixed; RE-OPENED.** PR #257 (`f144dea`)
>   fixed the originally-reported codename-rename case (the legacy-codename
>   backup fallback at `internal/api/demigrate.go:172-226`,
>   `tryLegacyPrefixRestore`, restores cross-rename entries with the
>   `restoredFrom == ""` guard preserving the #218 anti-deletion invariant).
>   But the codex bot on PR #259 flagged that the broad "должно работать
>   всегда" mandate is NOT fully met: `tryMarkerOrBackfillRemove` still fails
>   closed for a pre-marker hub-managed entry whose backups were all pruned
>   (the `allowURLBackfill = len(backups)>0 || sawLegacy` safety gate). The
>   close was reverted; the doc Status now carries the accept-vs-fix decision.
> - Row 14 (`2026-05-19-codex-config-symlink-blocked-by-pr209.md`, was
>   still-relevant-P2) — fixed by PR #217 (`9e89abe`, "Fix 1") and refined
>   by PR #258 (`44512e2`): `MCPHUB_ALLOW_CLIENT_CONFIG_SYMLINK` opt-in
>   (`internal/api/client_write_init.go:180,415`) + distinct `error-symlink`
>   scan category (`internal/api/scan.go:161`). Both of the doc's Suggested
>   fixes are implemented.
>
> Net effect, recomputed from the table (the original prose counts were
> off — see the Counts note below): still-relevant rows go from 10 (the
> original table's true count) down to **8** as rows 14 + 15 leave the
> bucket (row 13 was briefly counted closed, then re-opened); needs-repro
> rows go from the original table's true 5 down to **4** as row 12 leaves;
> close goes from 1 to **4** (rows 11, 12, 14, 15). The closures of 12, 14,
> 15 were stale phantom flags (each pr-review verdict read the bug doc's
> open `Status`, not HEAD source); row 13's close was reverted after the
> bot found the fix only partial.
>
> **Rows 6 + 10 — UPDATE 2026-06-14: row #6 CLOSED, row #10 STAYS OPEN.**
> `2026-05-08-test-portinuse-flake-install-audit-failclosed.md` (#6) and
> `2026-05-12-install-test-port-9128-collision.md` (#10) were two filings of the
> SAME port-9128 install-test flake. The residual this note flagged — a v0.5.0
> **supervisor child** (not a scheduled task) not being recognised as the
> legitimate port owner — is now FIXED: `internal/api/install.go:1760-1797`
> checks supervisor-intent (line 1761) BEFORE the scheduler-task fallback, so an
> own-daemon holding the port is recognised whether it is a scheduled task or a
> supervisor child (the `preflightPortInUse` seam, commit `5d91e42`). Adversarial
> re-verification (refute-default) found no residual on this axis; tests:
> `TestPreflight_AllowsSameSupervisorOwnedPortAndRejectsForeignIntentRow`
> (`install_own_port_test.go:146`),
> `TestPortHeldBySupervisorIntentDaemonExternalRequiresPIDProof`
> (`install_own_port_test.go:235`),
> `TestInstall_AuditFailsErrIdentityOversize_NoSchedulerMutation`
> (`install_intent_test.go:839`). **Row #6 moved to `closed/` 2026-06-14.**
>
> **Row #10 is NOT closed.** It tracks a DIFFERENT residual than the
> supervisor-child recognition #6 tracked: the audit-before-port-check ordering
> (the doc's Option-B preflight reorder — `Install` should short-circuit on an
> audit failure BEFORE any port probe so the audit-first guarantee in plan §62
> actually holds). That reorder is still deferred, so #10 stays OPEN. The two
> docs are no longer duplicates-with-identical-residual: #6's residual is gone,
> #10's ordering residual remains.

## Suggested next-action sequencing for the still-relevant entries

> Updated 2026-06-02: of the Demigrate/state-file cluster (#13, #14, #15)
> below, #14 + #15 are fully closed (already-fixed phantoms); **#13 was
> re-opened** — PR #257 fixed the reported codename-rename case but the broad
> "always-succeed" mandate isn't fully met (codex bot flagged it on PR #259).
> The remaining live still-relevant entries are rows 4, 5, 8, 10, 13,
> 16 (rows 6 + 7 CLOSED 2026-06-14 — see the reconciliation note; row 16 is
> PARTIALLY FIXED + RE-OPENED 2026-06-03 — #264 fixed its api-side leak but the
> gui-side leak remains; see the reconciliation note).

After PR #242 (job_protection surface + ADR + P3 nits), PR #243 (Phase 9
maintenance scheduler wiring), and PR #244 (configurable TerminateAll
timeout), the still-relevant bugs cluster by surface:

- **Demigrate always-succeed residual** (#13) — PARTIALLY closed: PR #257
  fixed the reported codename-rename case; the residual pre-marker +
  all-backups-pruned fail-closed case stays open (accept-vs-fix decision in
  the doc Status). The rest of the old cluster — #14 + #15 — is CLOSED
  2026-06-02 as already-fixed phantoms (PR #217 + #258 / PR #217
  inode-anchored read).
- **G3 cleanup** (#4, #5) — small bundled PR if G3 surface is touched
  for any reason.
- **G4 Phase 1 deferred cleanups** (#8) — opportunistic cleanup PR.
- **Test infrastructure** (#1, #10, #16) — port collision and
  test-leak hygiene. (#6 + #7 CLOSED 2026-06-14 — see the reconciliation note;
  #6's supervisor-child port-ownership residual fixed at `install.go:1760-1797`,
  #7's fn-pointer race fixed at `api_surfaces.go:119,160`. #10 stays open for the
  audit-before-port-check Option-B reorder. #16 tests-leak-state is PARTIALLY
  FIXED by #264 (api-side); its gui-side `TestRealClientInitializer` leak + #1
  flock-contention remain. The sibling api-symlink-dacl was fully closed
  2026-06-03 by #264.) Cluster into one test-infra hardening PR.

The remaining needs-repro entries should be probed during the next
test-infra PR (#1, #17, #18 — #12 closed 2026-06-02, #9 closed 2026-06-03
by #263, both as verified-fixed phantoms); if not reproducible against
master, move to `closed/`.

## Out of scope for this triage

- The 5 PR #241 backlog items (`job_protection: false` surface, ADR
  Status update, configurable TerminateAll timeout, P3 doc nits,
  cold-start reaper audit) are tracked separately as the PR #242-#245
  sequence per the 3-lane review synthesis in
  `.scratch/reviews/fix-design-2026-05-28-next-direction-after-pr241.md`.
- The 13 outstanding serena-supervisor-unified plan items (A.1, A.2,
  B.1, D.1, D.3, E.1, E.2, F.1-F.4, G.1, G.2, H.1, H.2, H.3) are not
  bugs and are tracked in
  `docs/superpowers/plans/2026-05-20-serena-supervisor-unified.md`.

---

## 2026-06-17 update — 6 rows CLOSED + moved to `bugs/closed/`

Resolved this session (or verified already-fixed earlier; status flipped + git-mv'd to `bugs/closed/`):

- Row 1 `api-tests-flock-contention` → CLOSED (Wave-3 TestMain state-dir fence 65d307f — api test binary defaults to a temp daemonStateRoot, no contention on the real lock).
- Row 2 `ci-bundle-freshness-gate` → CLOSED (#315 acf9f7f — ci.yml regen+`git diff --exit-code` of `internal/gui/assets/`).
- Row 3 `ci-missing-tagged-test-job` → CLOSED (#315 — tagged-test step + `go tool nm` symbol assertion).
- Row 10 `install-test-port-9128-collision` → CLOSED (already-fixed: `pickFreeLocalPort` + AST guard `TestNoLiveBandLiteralReachesKillOrListenSink`; verified).
- Row 16 `tests-leak-state-into-production-logs` → CLOSED (gui-side residual fixed: Wave-3 gui TestMain fence 65d307f + per-test `isolateStateDir` #264).
- `cli-supervise-statedir-override-ungated` → CLOSED (#318 prod gate + Wave-3 fence).

Also `2026-06-13-eventloop-test-data-race-priority-drain` → CLOSED (already fixed in #315 acf9f7f; mutex-guarded the test-side order slice). Net open count drops accordingly.
