# Plan: PR 288 Round 16 Review Fixes

## Scope

Fix the three review findings from `.scratch/codex-prompts/pr288-r16-findings.txt` on branch `codex-288r16-fixes`, limited to:

- `internal/api/register.go`
- `internal/api/register_supervisor.go`
- `internal/api/install_parsed_manifest.go`
- related `internal/api` tests

## Steps

| Step | Status | Evidence |
|---|---|---|
| Ground findings and existing contracts | Completed | Read review findings file and current owner code paths for unregister teardown, autostart owner handling, and LSP supervisor intent removal. |
| Add failing regression tests for F1-F3 | Completed | Red run failed for drifted autostart, foreign/mcphub port-owner unregister teardown, and stale stop suppressing supervised re-register. |
| Implement minimal fixes | Completed | Changed unregister to use `forceKillByPortFn`, drifted autostart to call `Enable`, and LSP descriptor removal to prune the matching stop in the same intent write. |
| Run required verification and state hash check | Completed | Native build, Linux amd64 cross-build, vet, scoped API tests, format check, and live state hash comparison all passed. |
| Write session log and commit once | Pending at snapshot time | Report and commit were the remaining closeout steps when this plan snapshot was written. |

## Terms and Abbreviations

- F1, F2, F3: The three review findings from PR 288 round 16.
- LSP: Language Server Protocol.
- MCPHub: The `mcphub` local supervisor and proxy binary in this repository.
- PR: Pull request.
