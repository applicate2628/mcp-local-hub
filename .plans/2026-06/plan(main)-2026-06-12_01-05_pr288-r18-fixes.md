Plan snapshot for PR #288 round 18 fixes.

1. Add failing regression tests for dry-run state safety and IPC orphan handling. Status: completed.
2. Implement non-mutating dry-run preview and IPC orphan pre-filter. Status: completed.
3. Run required verification and confirm live intent file unchanged. Status: blocked: verification commands passed, but live supervisor intent byte-identity check failed due a concurrent live `mcphub install` rewrite.
4. Write session report and commit without push. Status: blocked: session report written; commit withheld because the user-required state-safety gate did not pass.
