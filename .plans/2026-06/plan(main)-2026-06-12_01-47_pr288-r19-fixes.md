# PR #288 Round 19 Fix Plan

Status: PASS
Owner: main conversation
Canonical work-item: none; standalone user-directed fix task

1. Read `pr288-r19-findings.txt`, record live supervisor-intent baseline, and inspect the four owning code paths.
2. Add falsifying regression tests for F1-F4 and run them red.
3. Implement minimal owner-level fixes:
   - F1: manifest-server prefix fallback for blank-Server legacy daemon rows.
   - F2: native-http internal listener parent-chain proof with existing downgrade when lookup is unavailable.
   - F3: held supervisor lock plus failed IPC becomes a failed nudge, so removed-target kills are skipped.
   - F4: restore idle stop on IPC-unavailable wake nudge before releasing the in-flight marker.
4. Run required verification:
   - `go build ./...`
   - `GOOS=linux GOARCH=amd64 go build ./...`
   - `go vet ./...`
   - `go test ./internal/api/ -run 'InstallParsed|InstallPlanCore|OwnedBy|PortHeld|Preflight|Nudge|Autostart|WakeIdle|SerenaIdle' -count=1`
   - `gofmt -l <touched files>`
5. Verify `%LOCALAPPDATA%\mcp-local-hub\supervisor-intent.json` remains byte-identical to the start baseline: 13194 bytes, SHA256 `90EE5A757465FB2462F2DA454685CCDF4476168D87D237F3347F9CF72E692C7B`.
6. Commit one local commit and do not push.
