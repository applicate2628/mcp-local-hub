# PR 288 Round 17 Fix Plan

Execution role: main conversation
Assigned / replaced internal role: backend-engineer, qa-engineer
Requested provider: none
Resolved provider: none
Actual execution path: main Codex session
Model / profile used: unspecified by runtime
Deviation reason: none

## Goal
Fix two P2 review findings with falsifying Go regression tests, then run the requested build, vet, targeted tests, gofmt check, and local state byte-identity guard before one local commit.

## Plan
- [x] Verify the reported code paths against `internal/api/register_supervisor.go`, `internal/api/install_intent.go`, and `internal/cli/supervise_reconcile.go`.
- [x] Add F1 register tests for descriptor-absent active-stop re-enable before reconcile, including readiness-failure rollback restoring the prior stop.
- [x] Add F2 reconciler tests for running orphan LSP descriptor termination and registry-read-error fail-open no-op behavior.
- [x] Implement the smallest production changes: clear the register stop before reconcile/readiness while preserving rollback, and terminate running orphan LSP descriptors.
- [x] Run the required verification commands with the live `%LOCALAPPDATA%\mcp-local-hub\supervisor-intent.json` size/SHA256 guard.
- [x] Write `.reports/2026-06/` session log and commit once, without pushing.
