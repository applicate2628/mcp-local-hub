schema_version: 1
metadata:
  audit_register: audits/2026-08-12-audit-register.md
  source_audit_sha256: 5325de5e644dac96149ac94143c3e47d0895a678fddd031225f7413181f187b4
  count: 236
  note: Нормативные формулировки извлечены из неизменяемого аудита; изменения выполняются новым ID или amendment, а не молчаливой перезаписью.
requirements:
  SEC-001: "Все локальные HTTP listeners обязаны использовать `LocalIngressSecurity`"
  SEC-002: "`Host` проверяется по exact host+port allowlist; неизвестный host → `403`"
  SEC-003: "Присутствующий `Origin` проверяется по exact origin policy"
  SEC-004: "`Sec-Fetch-Site: cross-site` отклоняется"
  SEC-005: "`POST /mcp` принимает только допустимый JSON media type"
  SEC-006: "Body limit применяется до JSON decode; превышение → `413`"
  SEC-007: "Invalid JSON не пересылается upstream"
  SEC-008: "Owner token поддерживается для всех direct ports"
  SEC-009: "Секреты запрещены в URL query, logs и argv"
  SEC-010: "Новая listener registration без ingress policy должна быть compile/test failure"
  SEC-011: "Remote HTTP targets проходят URL/redirect/resolved-IP policy"
  SEC-012: "Unsafe local command tools регистрируются только после explicit opt-in"
  MCP-001: "Legacy и modern protocol adapters физически разделены"
  MCP-002: "Каждая рекламируемая версия имеет conformance suite"
  MCP-003: "Protocol version передаётся всем legacy requests"
  MCP-004: "Modern method/name headers соответствуют body"
  MCP-005: "Message framer возвращает typed errors"
  MCP-006: "Max message size задаётся manifest/policy и имеет global ceiling"
  MCP-007: "Notifications не получают JSON-RPC response body"
  MCP-008: "Cancellation переводит external ID во internal ID"
  MCP-009: "Server-to-client request имеет однозначного получателя"
  MCP-010: "Pagination обходится до конца с loop/limit protection"
  MCP-011: "Capability advertisement соответствует реально маршрутизируемым функциям"
  MCP-012: "Unsupported feature даёт явный protocol error, не silent drop"
  MCP-013: "Request delivery state сохраняется до terminal outcome"
  MCP-014: "Delivered mutating request не replay-ится без доказанной безопасности"
  MCP-015: "Legacy `2025-03-26` либо поддерживает batch, либо не рекламируется"
  SHARE-001: "Каждый manifest объявляет `sharing.scope`"
  SHARE-002: "Bidirectional backend не допускается в one-way shared mode"
  SHARE-003: "Project-scoped backend key строится из canonical root"
  SHARE-004: "Capability policy — identical/intersection/isolated — задаётся явно"
  SHARE-005: "Global mutable context запрещён для concurrent projects"
  SHARE-006: "Status показывает sharing key и число attached sessions"
  SHARE-007: "Idle reap выполняется по sharing instance, не по server name целиком"
  SHARE-008: "Migration не смешивает state/caches разных projects"
  SUP-001: "Все subprocess trees имеют проверяемый containment primitive"
  SUP-002: "Spawn/ready/exit имеют bounded deadlines"
  SUP-003: "IPC read и write имеют deadlines и frame limits"
  SUP-004: "PID-based destructive action связывается с start time/identity"
  SUP-005: "Backend framing error приводит к lifecycle failure/restart"
  SUP-006: "Quarantine имеет reason, evidence и explicit recovery"
  SUP-007: "Restart storm ограничивается backoff+jitter+budget"
  SUP-008: "POSIX process-tree fixture входит в integration suite"
  SUP-009: "macOS получает native smoke tests"
  SUP-010: "`doctor` проверяет containment capability текущего host"
  STATE-001: "Многошаговая destructive/mutating операция имеет journal"
  STATE-002: "Journal сохраняется до terminal committed/rolled_back state"
  STATE-003: "Crash recovery идемпотентен"
  STATE-004: "Reclaim crashed adopt является operator-driven"
  STATE-005: "Manifest deletion требует expected hash"
  STATE-006: "Secret deletion ограничена keys конкретной transaction"
  STATE-007: "Atomic writes синхронизируют file и parent dir, где поддерживается"
  STATE-008: "Uncertain committed outcome блокирует автоматический retry"
  STATE-009: "Все state files имеют schema version и migration"
  STATE-010: "Backup retention не удаляет единственный путь восстановления"
  SECRET-001: "Env placeholder не материализуется по умолчанию"
  SECRET-002: "Явный non-secret allow необходим для materialization"
  SECRET-003: "Предлагается преобразование env → vault reference"
  SECRET-004: "Secret provenance сохраняется без значения"
  SECRET-005: "Secret value не попадает в logs/errors/diffs"
  SECRET-006: "Secret operations сериализуются межпроцессно"
  SECRET-007: "Vault health видим в doctor/status"
  SECRET-008: "Rotation может атомарно перезапустить dependents"
  CLIENT-001: "Все adapters используют injectable path resolver"
  CLIENT-002: "Tests обязаны sandbox-ить каждый adapter path"
  CLIENT-003: "Forward mutation всегда имеет backup+journal"
  CLIENT-004: "Rollback проверяет expected hash/current ownership"
  CLIENT-005: "Новый adapter обязан иметь read/write/rollback fixtures"
  CLIENT-006: "Config mutation не выполняется до daemon readiness"
  CLIENT-007: "Unsupported schema не переписывается best-effort молча"
  CLIENT-008: "Scan/read path не должен изменять config"
  REL-001: "Любое ожидание внешнего процесса, сети, pipe или lock имеет deadline"
  REL-002: "Daemon crash обнаруживается и отражается в status"
  REL-003: "Oversize frame приводит к явному failure"
  REL-004: "Shutdown не блокируется бесконечно"
  REL-005: "Restart storm имеет бюджет"
  REL-006: "Отмена long-running tool освобождает owned subprocess"
  REL-007: "Crash transaction восстанавливается без ручного редактирования файлов"
  REL-008: "Один failing daemon не блокирует status остальных"
  REL-009: "Log writer degradation не блокирует control plane"
  REL-010: "Test suite не зависит от порядка"
  PERF-001: "P95 overhead hub для простого tool call без backend time"
  PERF-002: "`tools/list` fan-out ограничен"
  PERF-003: "Idle shared daemon не создаёт unbounded goroutines"
  PERF-004: "Log/event queues ограничены"
  PERF-005: "Status large fleet поддерживает pagination/streaming"
  PERF-006: "Project Serena instance может быть idle-reaped"
  PERF-007: "Frame parser не копирует payload многократно без необходимости"
  PERF-008: "Cold-start latency измеряется отдельно от request latency"
  OBS-001: "Каждый request имеет correlation ID, не содержащий secret"
  OBS-002: "Структурные события имеют schema version"
  OBS-003: "Transport error, protocol error и backend tool error различаются"
  OBS-004: "Метрики: active sessions, pending calls, frame errors, cancels, restarts, quarantines"
  OBS-005: "Drop counters имеют operator-visible warning"
  OBS-006: "Audit log health отображается в `status` и GUI"
  OBS-007: "Dependency source/version/digest доступен в `doctor`"
  OBS-008: "Uncertain operation отображается до explicit acknowledgement"
  OBS-009: "Logs redaction тестируется sentinel secrets"
  OBS-010: "Поддерживается экспорт diagnostic bundle без secret values"
  ARCH-001: "Продукт остаётся модульным монолитом с одним основным deployable binary"
  ARCH-002: "Production services создаются и связываются только в `internal/app`"
  ARCH-003: "`internal/cli` содержит только parsing, invocation и rendering"
  ARCH-004: "HTTP/GUI handlers не создают `api.NewAPI()` на каждый вызов"
  ARCH-005: "`internal/api` остаётся временным thin compatibility facade"
  ARCH-006: "Supervisor runtime принадлежит `internal/supervisor`"
  ARCH-007: "Install planner является чистым и детерминированным"
  ARCH-008: "Install executor исполняет только типизированный `InstallPlan`"
  ARCH-009: "Transaction/rollback logic имеет отдельного владельца"
  ARCH-010: "MCP transport, protocol, correlation, routing и recovery разделены интерфейсами"
  ARCH-011: "Один общий child-process lifecycle используется stdio/native HTTP adapters"
  ARCH-012: "Один общий registry snapshot/reload owner используется Serena/LSP"
  ARCH-013: "`LazyProxy` представляет состояния и события явными типами"
  ARCH-014: "Timeout ordering валидируется одним typed policy owner"
  ARCH-015: "Изменяемые package-level globals запрещены в production по умолчанию"
  ARCH-016: "Test doubles передаются через constructors/dependencies"
  ARCH-017: "Production packages не экспортируют setter/restore hooks только для тестов"
  ARCH-018: "Каждая goroutine имеет owner, cancellation и bounded join"
  ARCH-019: "Направления импортов проверяются architecture tests"
  ARCH-020: "Новый generic `utils`/`common` package без предметного владельца запрещён"
  ARCH-021: "Файл свыше установленного бюджета требует ADR/waiver"
  ARCH-022: "Исторические комментарии переносятся в ADR без потери security rationale"
  ARCH-023: "Большие Markdown/template bodies хранятся вне `.go`"
  ARCH-024: "Тесты группируются по устойчивому контракту, не по PR/раунду"
  ARCH-025: "Перед extraction добавляются characterization tests"
  ARCH-026: "Compatibility facade имеет owner, deadline и removal test"
  ARCH-027: "Public package surface имеет бюджет и API review"
  ARCH-028: "Error taxonomy не меняется скрыто при переносе между packages"
  ARCH-029: "Локальная верификация публикует отчёт размеров файлов, globals и архитектурных исключений"
  ARCH-030: "После каждого архитектурного инкремента удаляется старый owner либо фиксируется временное исключение"
  MAINT-001: "Один invariant реализуется одним package owner"
  MAINT-002: "История решений хранится в Architecture Decision Records (ADR; записи архитектурных решений), не в длинных comments"
  MAINT-003: "Public/internal interfaces имеют contract tests"
  MAINT-004: "Package-global mutable test seams постепенно заменяются dependency injection"
  MAINT-005: "Новые protocol handlers не читают ambient env напрямую"
  MAINT-006: "Generated files имеют freshness gate"
  MAINT-007: "Capability/documentation tables генерируются из source of truth"
  MAINT-008: "Critical package имеет owner в CODEOWNERS"
  COMPAT-001: "Поддерживаемые MCP versions перечислены машинно"
  COMPAT-002: "Legacy и modern clients проходят отдельные fixtures"
  COMPAT-003: "Client adapter version matrix хранит tested/live-smoke status"
  COMPAT-004: "Preview adapter не становится default target"
  COMPAT-005: "Manifest schema migration поддерживает dry-run и rollback"
  COMPAT-006: "Stable release сохраняет предыдущую minor config compatibility либо содержит migration tool"
  SUPPLY-001: "Все runtime dependencies immutable-pinned"
  SUPPLY-002: "`govulncheck` и dependency advisory checks входят в обязательный локальный gate"
  SUPPLY-003: "SBOM создаётся на release"
  SUPPLY-004: "Actions в единственном release-only npm workflow закреплены полными SHA"
  SUPPLY-005: "npm publishing использует OIDC trusted publisher"
  SUPPLY-006: "Release provenance связывает commit, локальный verification manifest, publish workflow и package"
  SUPPLY-007: "License inventory создаётся автоматически"
  SUPPLY-008: "Critical advisory имеет documented exception workflow с expiry"
  SUPPLY-009: "Platform package payload allowlist проверяется до publish"
  SUPPLY-010: "Release artifacts имеют reproducibility evidence или documented variance"
  UX-001: "GUI показывает разницу `running`, `healthy`, `degraded`, `quarantined`"
  UX-002: "Опасная операция показывает scope и irreversible effects до подтверждения"
  UX-003: "Uncertain committed outcome не маскируется обычной ошибкой"
  UX-004: "Project-scoped daemon отображает project root и attached clients"
  UX-005: "Protocol/version incompatibility имеет actionable remediation"
  UX-006: "Preview client/server явно маркируется и не выбирается по умолчанию"
  UX-007: "Diagnostic bundle создаётся одной командой/кнопкой"
  UX-008: "Секреты никогда не возвращаются в UI после сохранения; только state/presence"
  ST-001: "чужой `Host` на каждом loopback listener"
  ST-002: "чужой `Origin`"
  ST-003: "`Sec-Fetch-Site: cross-site`"
  ST-004: "отсутствующий owner token"
  ST-005: "неверный owner token"
  ST-006: "JSON в `text/plain`"
  ST-007: "`application/jsonx`"
  ST-008: "body `limit+1`"
  ST-009: "invalid JSON"
  ST-010: "DNS rebinding integration fixture"
  ST-011: "remote URL → loopback literal"
  ST-012: "remote URL → private DNS result"
  ST-013: "redirect public → private"
  ST-014: "secret in malformed URL"
  ST-015: "env secret with novel name"
  ST-016: "log sentinel secret"
  ST-017: "unpinned manifest"
  ST-018: "unsafe command tool without opt-in"
  ST-019: "unsafe command timeout"
  ST-020: "symlink/path traversal fixtures"
  PT-001: "legacy initialize"
  PT-002: "повторный initialize в той же session"
  PT-003: "subsequent request without session legacy"
  PT-004: "version mismatch session/header"
  PT-005: "modern `server/discover`"
  PT-006: "modern request headers/body mismatch"
  PT-007: "`_meta` forwarding"
  PT-008: "two clients same request ID"
  PT-009: "cancel after delivery"
  PT-010: "cancel before delivery"
  PT-011: "context disconnect без cancellation notification"
  PT-012: "response races child exit"
  PT-013: "server notification"
  PT-014: "server reverse request"
  PT-015: "unsupported reverse capability"
  PT-016: "`tools/list` 3 pages"
  PT-017: "cursor loop"
  PT-018: "page count limit"
  PT-019: "legacy batch `2025-03-26`"
  PT-020: "`stdio` frame `limit+1`"
  PT-021: "SSE frame `limit+1`"
  PT-022: "multi-line SSE data"
  PT-023: "malformed upstream JSON"
  PT-024: "mutating ambiguous delivery"
  PT-025: "read-only proven pre-delivery failure"
  LT-001: "128 concurrent requests"
  LT-002: "129th request"
  LT-003: "256 sessions + 1"
  LT-004: "slow SSE subscribers"
  LT-005: "child exits while pipe held by grandchild"
  LT-006: "parent force-killed"
  LT-007: "IPC client does not read response"
  LT-008: "repeated crash storm"
  LT-009: "restart during in-flight call"
  LT-010: "shutdown with audit lock contention"
  LT-011: "`go test -shuffle=on -count=10`"
  LT-012: "race detector"
  LT-013: "goroutine leak check"
  LT-014: "dynamic Serena projects"
  LT-015: "concurrent project A/B edits"
  K-001: "объединение transports ломает редкого клиента"
  K-002: "owner token не поддерживается некоторыми клиентами"
  K-003: "Serena project pool увеличит память"
  K-004: "modern MCP specification ещё будет меняться"
  K-005: "рефакторинг супервизора создаст lifecycle regression"
  K-006: "strict dependency pins замедлят security updates"
  K-007: "OIDC multi-package publish даст partial release"
  K-008: "defect registry migration потеряет историю"
  K-009: "audit writer сам станет single point of failure"
  K-010: "protocol sharing policy усложнит manifests"
  K-011: "package extraction создаст import cycles"
  K-012: "чрезмерная абстракция ухудшит читаемость"
  K-013: "крупные moves осложнят security backports"
  K-014: "очистка комментариев потеряет forensic context"
  K-015: "architecture gates превратятся в формальность"
